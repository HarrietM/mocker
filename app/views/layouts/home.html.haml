%div.navbar
  .profile
    .item= current_user.username
    .item= current_user.email
    = link_to "Sign out", destroy_user_session_path, method: :delete

%a{href: "#tweets"} Show Tweets
%a{href: "#follows"} People I am Following
%ng-view